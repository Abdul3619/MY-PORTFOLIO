// Background animation is managed globally and persistently via BackgroundProvider
// to survive route changes and component re-renders without flickering or breakdown.
export default function Background() {
  return null;
}
